{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agent-teams.ai/engineering-foundation/repository-security-baseline/v1",
  "title": "Repository security baseline capability configuration",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "workflowDirectory",
    "dependencyReview",
    "sbomWorkflow",
    "allowedContainerImages",
    "allowedUses",
    "privilegedJobs",
    "publishablePackageManifests"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "workflowDirectory": { "$ref": "#/$defs/repositoryPath" },
    "dependencyReview": { "$ref": "#/$defs/dependencyReviewPolicy" },
    "sbomWorkflow": { "$ref": "#/$defs/repositoryPath" },
    "allowedContainerImages": {
      "type": "array",
      "maxItems": 500,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/pinnedContainerImage" }
    },
    "allowedPullRequestTargetWorkflows": {
      "type": "array",
      "maxItems": 100,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/repositoryPath" }
    },
    "allowedUses": {
      "type": "array",
      "maxItems": 500,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/allowedWorkflowUse" }
    },
    "privilegedJobs": {
      "type": "array",
      "maxItems": 100,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["workflowPath", "jobId", "permissions"],
        "properties": {
          "workflowPath": { "$ref": "#/$defs/repositoryPath" },
          "jobId": { "type": "string", "pattern": "^[A-Za-z0-9_-]{1,100}$" },
          "permissions": { "$ref": "#/$defs/permissions" }
        }
      }
    },
    "publishablePackageManifests": {
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/repositoryPath" }
    },
    "toolEvidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["actionlint", "zizmor"],
      "properties": {
        "actionlint": { "$ref": "#/$defs/toolEvidencePolicy" },
        "zizmor": { "$ref": "#/$defs/toolEvidencePolicy" },
        "codeql": { "$ref": "#/$defs/toolEvidencePolicy" }
      }
    }
  },
  "$defs": {
    "repositoryPath": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$))(?!.*\\\\)(?!.*//)[^\\u0000]+$"
    },
    "permissions": {
      "type": "object",
      "minProperties": 1,
      "maxProperties": 30,
      "propertyNames": { "pattern": "^[a-z-]{1,50}$" },
      "additionalProperties": { "enum": ["none", "read", "write"] }
    },
    "pinnedExternalUse": {
      "type": "string",
      "maxLength": 1000,
      "anyOf": [
        {
          "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+(?:/[A-Za-z0-9_./-]+)?@[0-9a-fA-F]{40}$"
        },
        {
          "pattern": "^docker://.+@sha256:[0-9a-fA-F]{64}$"
        }
      ]
    },
    "pinnedContainerImage": {
      "type": "string",
      "maxLength": 1000,
      "pattern": "^(?!docker://)[A-Za-z0-9][A-Za-z0-9._/-]*(?::[A-Za-z0-9][A-Za-z0-9._.-]*)?@sha256:[a-fA-F0-9]{64}$"
    },
    "allowedWorkflowUse": {
      "type": "object",
      "additionalProperties": false,
      "required": ["uses", "transitiveUses"],
      "properties": {
        "uses": { "$ref": "#/$defs/pinnedExternalUse" },
        "transitiveUses": {
          "type": "array",
          "maxItems": 100,
          "items": { "$ref": "#/$defs/allowedWorkflowUse" }
        }
      }
    },
    "dependencyReviewPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["workflowPath", "jobId", "baseRef", "headRef", "failOnSeverity"],
      "properties": {
        "workflowPath": { "$ref": "#/$defs/repositoryPath" },
        "jobId": { "type": "string", "pattern": "^[A-Za-z0-9_-]{1,100}$" },
        "baseRef": { "type": "string", "minLength": 1, "maxLength": 1000 },
        "headRef": { "type": "string", "minLength": 1, "maxLength": 1000 },
        "failOnSeverity": { "enum": ["low", "moderate"] }
      }
    },
    "toolEvidencePolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "configPath",
        "evidencePath",
        "invocationUse",
        "jobId",
        "resultPath",
        "rollout",
        "version",
        "workflowPath"
      ],
      "properties": {
        "configPath": { "$ref": "#/$defs/repositoryPath" },
        "evidencePath": { "$ref": "#/$defs/repositoryPath" },
        "invocationUse": { "$ref": "#/$defs/pinnedExternalUse" },
        "jobId": { "type": "string", "pattern": "^[A-Za-z0-9_-]{1,100}$" },
        "resultPath": { "$ref": "#/$defs/repositoryPath" },
        "rollout": { "enum": ["advisory", "blocking"] },
        "version": {
          "type": "string",
          "pattern": "^v?[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
        },
        "workflowPath": { "$ref": "#/$defs/repositoryPath" }
      }
    }
  }
}
