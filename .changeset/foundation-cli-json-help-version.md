---
"@agent-teams/engineering-foundation": patch
---

Reject --json for the help and version commands with the standard invalid-input JSON envelope instead of silently ignoring the flag and writing plain text.
