// Historical names are persisted wire-format authority and cannot be renamed.
export const LOCAL_STATE_DIRECTORY = ".agent-teams-local" as const;
export const FOUNDATION_TRANSACTION_FILE = "scaffolding-transaction.json" as const;
