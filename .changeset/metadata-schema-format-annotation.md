---
"@agent-teams/document-authoring": patch
"@agent-teams/engineering-foundation": patch
---

Document that the JSON Schema \`format\` keyword in these schema catalogs is a reader/tooling annotation, not a runtime-enforced constraint, since \`validateFormats\` is intentionally disabled. No behavior change.
