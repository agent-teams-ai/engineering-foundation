name: ReviewRouter Release Review

run-name: ${{ format('ReviewRouter release review PR {0} at {1}', inputs.pr_number, inputs.review_head_sha) }}

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: Release pull request number
        required: true
        type: string
      review_head_sha:
        description: Exact release pull request head SHA
        required: true
        type: string

permissions: {}

jobs:
  codex-review:
    name: codex-review
    permissions:
      contents: read
      pull-requests: read
      id-token: write
    uses: 777genius/review-router/.github/workflows/reviewrouter-t0-reusable.yml@e982a5e3aebe71404dac948fd67cb7291e8be0fb
    with:
      runtime_ref: "e982a5e3aebe71404dac948fd67cb7291e8be0fb"
      api_url: "https://api.reviewrouter.site"
      runtime_config_mode: oidc
      static_runtime_env_json: '{"SKIP_BOTS":"false"}'
      pr_number: ${{ inputs.pr_number }}
      review_head_sha: ${{ inputs.review_head_sha }}
      provider_instance_id: "codex-rotating:1316243988"
      workflow_schema_version: 2
      max_changed_lines: ${{ vars.REVIEW_ROUTER_MAX_CHANGED_LINES }}
      review_timeout_minutes: ${{ fromJSON(vars.REVIEW_ROUTER_TIMEOUT_MINUTES || '60') }}
    secrets:
      CODEX_AUTH_JSON: ${{ secrets.REVIEWROUTER_CODEX_AUTH_JSON }}
