---
"@agent-teams/engineering-foundation": minor
"@agent-teams/docs-protocol": minor
---

Add the versioned Foundation directory-materialization kernel and the separate
unified documentation protocol package. Provide one deterministic authoring,
query, validation, and recovery workflow backed by consumer-owned data-only
profiles, explicit reachability, relation metadata, and portable transactional
filesystem guarantees.
