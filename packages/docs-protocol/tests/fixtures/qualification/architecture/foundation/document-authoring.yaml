schemaVersion: 2
projectId: docs-protocol-qualification
catalog:
  metadataSchemaPath: docs/metadata.schema.json
  ownerCatalog: {path: docs/owners.yaml, contract: foundation.owner-map/v1}
  collections:
    - {kind: markdown-tree, root: docs/decisions}
  excludedPrefixes: [docs/templates]
authoring:
  mode: create-only
  artifactTypes:
    - type: adr
      initialStatus: proposed
      identity: {kind: explicit, format: adr-four-digits}
      placement: {kind: collection, directory: docs/decisions/generated, filename: numeric-id-slug}
      template: {kind: fenced-markdown-body, path: docs/templates/adr.md}
      heading: {kind: id-colon-title}
      reachability: {kind: manual-fixed-index, indexPath: docs/decisions/README.md}
      allowedOwnerIds: [architecture/tooling]
