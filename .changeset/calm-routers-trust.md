---
"@agent-teams/engineering-foundation": patch
---

Allow repository security policy to declare exact reviewed workflow paths that use `pull_request_target`, while rejecting undeclared and stale privileged-trigger routes.
